# GrantPilot Dashboard (Vite + React + TypeScript)

## Setup

```bash
npm install
npm run dev
```

The main entry mounts `GrantPilotDashboard` from `src/GrantPilotDashboard.tsx`.
TailwindCSS is configured via `tailwind.config.cjs` and `postcss.config.cjs`.
